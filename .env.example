# All credentials in this file need to be defined as environment secrets when running
# in github or any other CI or production envionment. 

# Eagle Eye Networks OAuth2 Configuration
# Get these values from your Eagle Eye Networks developer account
VITE_EEN_CLIENT_ID=your_client_id_here
VITE_EEN_CLIENT_SECRET=your_client_secret_here

# OAuth2 Redirect URI
# This must match the redirect URI configured in your Eagle Eye Networks application
# The value below is the default
#VITE_REDIRECT_URI=http://127.0.0.1:3333  

# OPTIONAL: Eagle Eye Networks endpoints for login and tokens. the values below are the defaults
#VITE_EEN_AUTH_URL="https://auth.eagleeyenetworks.com/oauth2/authorize"
#VITE_EEN_TOKEN_URL="https://auth.eagleeyenetworks.com/oauth2/token"

#VITE_AUTH_PROXY_URL="https://YOUR-WORKER.YOUR-CLOUDFLARE-DOMAIN.workers.dev"
# default for VITE_AUTH_PROXY_URL is the local vite proxy "http://127.0.0.1:3333"

# Playwright test credentials 
TEST_USER="email address of the test user"
TEST_PASSWORD="password of the test user"

# SLACK configuration, if you want to use Slack for updates.
# prepare a slack web hook and define it below. There is a test script in ./scripts
# Other than that, this webhook needs to be a Github action secret
SLACK_WEBHOOK_URL=https://.... your slack webhook here
